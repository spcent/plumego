# Card 0714

Milestone: cmd stable hardening
Recipe: specs/change-recipes/fix-bug.yaml
Priority: P0
State: active
Primary Module: cmd/plumego
Owned Files: cmd/plumego/commands/build.go, cmd/plumego/commands/check.go, cmd/plumego/internal/devserver/builder.go, cmd/plumego/commands/cli_e2e_test.go
Depends On:

Goal:
Make the projects generated by `plumego new` work with the default `plumego dev`,
`plumego build`, and `plumego check` commands.

Scope:
- Detect the canonical `cmd/app` entrypoint when no root `main.go` exists.
- Build `./cmd/app` by default for generated canonical projects.
- Align structure checks with canonical project layout.
- Add focused CLI tests that cover generated project defaults.

Non-goals:
- Do not redesign templates.
- Do not add new dependencies.
- Do not change stable core APIs.
- Do not change scenario template contents beyond what is needed for defaults.

Files:
- `cmd/plumego/commands/build.go`
- `cmd/plumego/commands/check.go`
- `cmd/plumego/internal/devserver/builder.go`
- `cmd/plumego/commands/cli_e2e_test.go`

Tests:
- `go test ./commands ./internal/devserver`
- `go build .`

Docs Sync:
None unless command behavior text changes.

Done Definition:
- `plumego new <app>` output has a default entrypoint that `dev`, `build`, and
  `check` understand without extra flags.
- Relevant tests pass.

Outcome:

