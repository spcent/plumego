# Verify M-001: v1 Trust Baseline

Milestone: `M-001`
Branch: `codex/v1-release-readiness`
Verified Cards:

- `tasks/milestones/active/M-001.md`

## Scope Check

- In-scope files touched:
  - `.github/workflows/quality-gates.yml`
  - `.gitignore`
  - `Makefile`
  - `docs/release/PRE_V1_RELEASE_CHECKLIST.md`
  - `docs/release/v1.0.0-rc.1.md`
  - `website/src/generated/roadmap.ts`
- Out-of-scope files touched:
  - none

## Ownership Check

- overlapping card ownership:
  - none in `tasks/cards/active/`
- unresolved ownership conflicts:
  - none

## Symbol Completeness Check

- exported symbol changes:
  - none
- residual reference grep:
  - not applicable

## CLI Template Trust Matrix

| Template | Command | Result |
| --- | --- | --- |
| `canonical` | `cd cmd/plumego && go run . new --template canonical --dry-run trust-check` | PASS |
| `rest-api` | `cd cmd/plumego && go run . new --template rest-api --dry-run trust-check` | PASS |
| `tenant-api` | `cd cmd/plumego && go run . new --template tenant-api --dry-run trust-check` | PASS |
| `invalid-template-name` | `cd cmd/plumego && go run . new --template invalid-template-name --dry-run trust-check` | PASS, exits non-zero with clear valid-template list |

## Release Claim Audit

- local tags:
  - none before this release-readiness pass
- remote tags:
  - none before this release-readiness pass
- release status:
  - repository remains pre-v1 until `v1.0.0-rc.1` is tagged and pushed
- extension status:
  - all `x/*` module-level surfaces remain experimental unless explicitly listed otherwise

## Module Test Summary

- primary module tests:
  - `cd cmd/plumego && go vet ./...`: PASS
  - `cd cmd/plumego && go test -race -timeout 60s ./...`: PASS
  - `cd cmd/plumego && go test -timeout 20s ./...`: PASS
- secondary module tests:
  - root gates recorded in `docs/release/v1.0.0-rc.1.md`

## Boundary Check Summary

- dependency-rules: PASS
- agent-workflow: PASS
- module-manifests: PASS
- reference-layout: PASS

## Repo Gate Summary

- `go test -race -timeout 60s ./...`: PASS
- `go test -timeout 20s ./...`: PASS
- `go vet ./...`: PASS
- `gofmt -l .`: PASS, no output

## Open Issues

- none for M-001 after `v1.0.0-rc.1` tag creation

## Final Verdict

- `PASS`
- rationale: CLI template behavior, release-claim audit, and release-readiness gate alignment have recorded evidence.
