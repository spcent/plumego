# M-024: Stable-Tier Metadata and Control-Plane Clarity

- **Branch:** `milestone/M-024-stable-tier-metadata-control-plane`
- **Depends on:** M-022, M-023
- **Parallel OK:** no

---

## Goal

The remaining non-runtime audit findings after `x/ai` resilience convergence are
made explicit: stable-tier AI subpackages gain machine-readable manifests, and
the still-reproducible docs/specs/tasks ambiguity is cleaned up without
reopening the `x/ai/resilience` runtime migration.

---

## Architecture Decisions

- Runtime dual-stack convergence belongs to M-023 and must land first.
- Stable-tier AI metadata should be machine-readable at subpackage granularity
  where the repo already treats those surfaces as beta-oriented entrypoints.
- Internal and external agent-first docs must not differ only by filename case.
- Control-plane history should document what exists on disk rather than imply
  missing archive artifacts.

## Context — Read Before Touching Code

1. `AGENTS.md`
2. `docs/CODEX_WORKFLOW.md`
3. `specs/task-routing.yaml`
4. `specs/dependency-rules.yaml`
5. `x/ai/module.yaml`
6. `docs/modules/x/ai/README.md`
7. `docs/README.md`
8. `tasks/milestones/active/M-024-stable-tier-metadata-and-control-plane-clarity/plan-M-024.md`

## Affected Modules

- **Primary:** `tasks/milestones`
- **Secondary:** `x/ai`, `docs`, `specs`, `middleware`, `tasks`

---

## Tasks

### Phase 1 — Normalize Stable-Tier AI Metadata (sequential)

1. Execute cards `2062` and `2063`.

### Phase 2 — Clean Up Docs and Specs Ambiguity (sequential)

2. Execute cards `2064`, `2065`, and `2066`.

### Phase 3 — Reconcile Task-History Clarity (sequential)

3. Execute card `2067`.

### Phase 4 — Validate and Ship (sequential)

4. Run the Acceptance Criteria commands below after the final card lands.
5. Run `gofmt -w .` and confirm `gofmt -l .` is empty.
6. Commit card-level changes with `[M-024]`, then finish with
   `milestone(M-024): stable-tier metadata and control-plane clarity`.
7. Push `milestone/M-024-stable-tier-metadata-control-plane` and open a PR
   using `docs/github-workflows/milestone-pr-template.md`.

---

## Acceptance Criteria

```bash
go run ./internal/checks/dependency-rules
go run ./internal/checks/agent-workflow
go run ./internal/checks/module-manifests
go run ./internal/checks/extension-maturity
go test -timeout 20s ./x/ai/... ./middleware/...
gofmt -l .
```

Expected: all exit 0; `gofmt -l .` outputs nothing.

---

## Out of Scope

- Do not re-open `x/ai/resilience` runtime migration here.
- Do not add external dependencies.
- Do not rewrite historical milestone intent; add archive notes instead.

---

## Open Questions

(none at spec time)

---

## Done Definition

- [x] All Acceptance Criteria commands exit 0.
- [x] `gofmt -l .` produces no output.
- [x] `plan-M-024.md` exists in this milestone directory and matches the milestone scope.
- [x] `verify-M-024.md` is filled in this milestone directory before PR packaging.
- [ ] Branch `milestone/M-024-stable-tier-metadata-control-plane` pushed.
- [ ] PR open, title `milestone(M-024): Stable-Tier Metadata and Control-Plane Clarity`.
- [ ] PR body filled from milestone-pr-template.md with actual gate output.
- [x] Architecture Decisions section: no violations logged.
