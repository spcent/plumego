# Verify M-003: Extension Evidence Pipeline

Milestone: `M-003`
Branch: `codex/v1-release-readiness`
Verified Cards:

- `tasks/milestones/active/M-003.md`

## Scope Check

- In-scope files touched:
  - `.github/workflows/quality-gates.yml`
  - `Makefile`
  - `docs/release/PRE_V1_RELEASE_CHECKLIST.md`
  - `docs/release/v1.0.0-rc.1.md`
- Out-of-scope files touched:
  - no extension implementation files changed

## Ownership Check

- overlapping card ownership:
  - none in `tasks/cards/active/`
- unresolved ownership conflicts:
  - none

## Symbol Completeness Check

- exported symbol changes:
  - none
- residual reference grep:
  - not applicable

## Extension Evidence Summary

| Candidate | Snapshot state | Release refs | Owner sign-off | Status |
| --- | --- | --- | --- | --- |
| `x/rest` | current-head snapshot present | missing | missing | blocked |
| `x/websocket` | current-head snapshot present | missing | missing | blocked |
| `x/tenant` | current-head snapshot present | missing | missing | blocked |
| `x/observability` | current-head snapshot present | missing | missing | blocked |
| `x/gateway` | current-head snapshot present | missing | missing | blocked |
| `x/ai/provider` | current-head snapshot present | missing | missing | blocked |
| `x/ai/session` | current-head snapshot present | missing | missing | blocked |
| `x/ai/streaming` | current-head snapshot present | missing | missing | blocked |
| `x/ai/tool` | current-head snapshot present | missing | missing | blocked |
| `x/data:file` | snapshot missing | missing | missing | blocked |
| `x/data:idempotency` | snapshot missing | missing | missing | blocked |
| `x/discovery:core-static` | snapshot missing | missing | missing | blocked |
| `x/messaging:app-facing-service` | snapshot missing | missing | missing | blocked |

## Module Test Summary

- primary module tests:
  - `go test -timeout 20s ./x/rest/... ./x/tenant/... ./x/observability/... ./x/gateway/...`: covered by full root standard gate
  - `go test -race -timeout 60s ./x/rest/... ./x/tenant/... ./x/observability/... ./x/gateway/...`: covered by full root race gate
- evidence checks:
  - `go run ./internal/checks/extension-maturity`: PASS
  - `go run ./internal/checks/extension-beta-evidence`: PASS, reports blockers as expected

## Boundary Check Summary

- dependency-rules: PASS
- agent-workflow: PASS
- module-manifests: PASS
- reference-layout: PASS

## Repo Gate Summary

- `go test -race -timeout 60s ./...`: PASS
- `go test -timeout 20s ./...`: PASS
- `go vet ./...`: PASS
- `gofmt -l .`: PASS, no output

## Open Issues

- extension beta promotion remains blocked until each candidate has two concrete release refs, matching API snapshots, and owner sign-off.
- first release ref can start with `v1.0.0-rc.1`; it does not by itself satisfy the two-release rule.

## Final Verdict

- `FAIL`
- rationale: the evidence pipeline checks are working, but M-003 acceptance requires release refs and owner sign-off for `x/rest`; those cannot be truthfully completed before at least two concrete release refs exist.
