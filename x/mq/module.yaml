name: x/mq
path: x/mq
layer: extension
status: experimental
summary: Subordinate durable queue primitives and worker coordination used by the broader x/messaging capability pack.

parent_family: x/messaging

responsibilities:
  - Queue primitives
  - Task persistence
  - Worker coordination
  - Dead-letter handling

non_goals:
  - Core bootstrap ownership
  - Stable root entrypoints
  - Business workflow orchestration

allowed_imports:
  - contract
  - log
  - metrics
  - store
  - x/pubsub

forbidden_imports:
  - core/components/**
  - x/tenant/**
  - x/ai/**

test_commands:
  - go test -race -timeout 60s ./x/mq/...
  - go test -timeout 20s ./x/mq/...
  - go vet ./x/mq/...

doc_paths:
  - docs/modules/x-mq/README.md

review_checklist:
  - Keep queue semantics explicit
  - Avoid hidden background side effects
  - Preserve persistence correctness

agent_hints:
  - Treat x/mq as a subordinate queue primitive; start app-facing feature discovery in x/messaging
  - Keep queue adapters local with explicit worker wiring
  - Do not use x/mq as a competing family entrypoint for new messaging work or move it into core
