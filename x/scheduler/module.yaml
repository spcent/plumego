name: x/scheduler
path: x/scheduler
layer: extension
status: experimental
summary: Subordinate in-process scheduling primitives for cron, delayed jobs, and retries within the broader x/messaging capability family.

responsibilities:
  - Scheduler primitives
  - Delayed job execution
  - Retry coordination
  - Cron helpers

non_goals:
  - Core bootstrap ownership
  - Stable root entrypoints
  - Business workflow orchestration

allowed_imports:
  - log
  - metrics
  - store

forbidden_imports:
  - core/components/**
  - x/tenant/**
  - x/ai/**

test_commands:
  - go test -race -timeout 60s ./x/scheduler/...
  - go test -timeout 20s ./x/scheduler/...
  - go vet ./x/scheduler/...

doc_paths:
  - docs/modules/x-scheduler/README.md

review_checklist:
  - Keep scheduling behavior explicit
  - Avoid hidden process-wide state
  - Preserve retry determinism

agent_hints:
  - Treat x/scheduler as a subordinate scheduling primitive; start app-facing messaging feature discovery in x/messaging
  - Open x/scheduler directly only for narrow cron, delayed job, or retry coordination work
  - Do not use x/scheduler as a competing family entrypoint for new messaging work or move it into core
