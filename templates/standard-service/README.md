# Standard Service Template

`templates/standard-service` is the reserved scaffold target that must mirror
`reference/standard-service` as the canonical generated project shape.

Rules:

- keep directory and file ownership aligned with `reference/standard-service`
- do not introduce alternate bootstrap patterns here
- update this template only alongside the reference app and scaffold logic
