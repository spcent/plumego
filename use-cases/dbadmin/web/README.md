# dbadmin web

React + TypeScript + Vite frontend for the dbadmin use-case app.

## Commands

```bash
npm ci
npm run dev
npm run lint
npm test
npm run build
```

The project uses npm as the canonical package manager. Keep
`package-lock.json` in sync when dependencies change.
