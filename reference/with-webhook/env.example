# with-webhook environment variables
# Copy this file to .env for local development.
# Process environment variables override .env; command-line flags override both.

# Listen address (default: :8085)
# APP_ADDR=:8085

# Enable verbose request/response logging for webhook routes (default: false)
# APP_DEBUG=false

# Path to the .env file loaded at startup (default: .env in the working directory).
# APP_ENV_FILE=.env
