# with-rpc environment variables
# Copy this file to .env for local development.
#
# NOTE: this reference service uses an in-process bufconn transport for the gRPC
# layer and binds the HTTP gateway on a random OS-assigned port. There are no
# runtime configuration knobs — the service is intentionally hard-coded for demo
# purposes. No environment variables are required or read at startup.
