# x/openapi

`x/openapi` generates OpenAPI 3.1 document structures from explicit
`router.RouteInfo` values and optional operation hints.

The module is experimental. It does not inspect handler functions and does not
depend on an external OpenAPI library. Serialization and CLI wiring are handled
by later OpenAPI milestone cards.
