# Archived Draft: Agent-Native Roadmap

This file is kept only as a historical pointer.

The original roadmap described a transition from an agent-aware repository to an
agent-native operating model. Much of that transition has since moved into the
active repository roadmap, milestone workflow, task-card queue, and internal
checks.

## Current Authority

Use these documents instead:

- `docs/ROADMAP.md` — active roadmap and phase tracking
- `docs/CODEX_WORKFLOW.md` — milestone execution workflow
- `docs/MILESTONE_PIPELINE.md` — artifact-level handoff contract
- `tasks/milestones/*` — current milestone queue and templates
- `tasks/cards/*` — active execution cards
- `specs/*` — machine-readable control plane and boundary rules

## Why This Draft Was Archived

- it remained a planning draft after the workflow had already been operationalized
- multiple sections described gaps that are now closed or no longer phrased correctly
- the repository now uses milestone specs, cards, and checks as the live execution surface instead of a prose-only transformation roadmap

## Historical Note

Keep this path for backward references from archived cards and planning notes,
but do not use it for current sequencing or status decisions.
