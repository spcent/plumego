# Plumego v1.0.0

## Status

- Final decision: GO on May 15, 2026.
- Final tag: ready to create from the release decision commit.
- Blocking item: none found during the `v1.0.0-rc.1` observation window.

## Evidence Reviewed

- Local `make gates` passed on May 15, 2026.
- Remote GitHub Actions run `25920615874` passed for `v1.0.0-rc.1`.
- Stable-root final freeze evidence is recorded in `tasks/milestones/M-005.verify.md`.
- CLI onboarding evidence is recorded in `tasks/milestones/M-005.verify.md`.
- Extension maturity evidence is recorded in `tasks/milestones/M-005.verify.md`.
- RC observation evidence is recorded in `tasks/milestones/M-005.verify.md`.

## Decision

Tag final `v1.0.0` from the release decision commit after this document and the
M-005 verify artifact are committed. The `v1.0.0-rc.1` remote gates passed, no
open GitHub issues were present during the observation check, and no P0/P1
release blocker was found.

## Tagging

```bash
git tag -a v1.0.0 -m "Plumego v1.0.0"
git push origin HEAD:refs/heads/codex/v1-release-execution v1.0.0
```
