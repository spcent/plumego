# Plumego v1.0.0-rc.1

## Status

- Main module: release candidate for v1.
- Stable roots: GA candidate surface for `core`, `router`, `contract`, `middleware`, `security`, `store`, `health`, `log`, and `metrics`.
- Canonical reference app: supported reference aligned with stable-root usage.
- CLI: v1 hardening scope, supported as a command-line tool and not as a Go import surface.
- Extensions: `x/gateway`, `x/observability`, `x/rest`, and `x/websocket`
  are beta extension families. All other `x/*` modules remain experimental
  unless their module manifest explicitly records another status.

## Included

- CI and local release gates now use the same live control-plane paths.
- Local `make gates` now includes extension maturity checks, beta-evidence checks, stable-root coverage, `cmd/plumego` submodule checks, and website generated-data validation.
- Local `make gates` now includes the deprecation inventory check for registered compatibility aliases, unsupported-operation placeholders, and TODO guardrails.
- Website release and roadmap generated data is synchronized from repository source documents.
- Milestone verification artifacts record M-001 trust baseline, M-002 stable-root freeze evidence, and M-003 extension evidence blockers.

## Compatibility Decisions

- `middleware/httpmetrics.Observer` is retained for v1 as the app-facing alias for `metrics.HTTPObserver`; this keeps HTTP metrics middleware wiring local to the middleware package.
- `reference/workerfleet` is not part of the canonical v1 reference surface. Its unimplemented worker registration path remains a non-blocking reference placeholder.
- `x/*` compatibility aliases remain extension-scoped and are not stable-root
  API. This includes beta `x/gateway` gateway-prefixed entrypoint aliases plus
  experimental `x/messaging` family entrypoint aliases, `x/mq` pubsub
  re-exports, `x/data/idempotency` stable-contract re-exports, and
  `x/ai/multimodal` provider type aliases.
- Experimental `x/*` compatibility helpers remain non-stable. `x/ipc.RemoteAddrString` is retained as a migration helper; typed `Client.RemoteAddr().String()` remains preferred.
- Experimental `x/mq` MQTT and AMQP bridge entrypoints remain explicit unsupported operations for v1. `Config.Validate` fails closed when those bridges are enabled.
- Experimental `x/mq` ack and trie defaults remain compatibility-preserving opt-in behavior for v1.

## Evidence

- Commit: annotated tag target for `v1.0.0-rc.1` is `a2346813`; local annotated
  tag object is `151770bf`.
- Branch used for preparation: `main` during M-005 local release execution.
- Gate parity: GitHub Actions, `make gates`, and `specs/checks.yaml` all use the same live boundary, maturity, beta-evidence, deprecation, CLI, and website-generated-data checks.
- Stable-root freeze audit: all stable root manifests are `status: ga`, checked-in snapshots match current exported APIs, production code has no `x/*` imports, and retained stable compatibility aliases are registered in `specs/deprecation-inventory.yaml`.
- Deprecation inventory: strict mode enforces explicit category, status, owner, decision, replacement, and path metadata for retained compatibility and deprecated markers.
- Security fail-closed audit: stable auth, middleware, and contract negative
  paths plus security-adjacent `x/webhook`, `x/websocket`, and `x/gateway`
  paths were validated. `x/websocket` and `x/gateway` are beta extension
  families; `x/webhook` remains experimental.
- Reference and CLI audit: `reference/standard-service` remains the canonical v1 application wiring example, `reference/workerfleet` is explicitly non-canonical, and `cmd/plumego` remains a supported command-line tool rather than a Go import surface.
- Extension maturity lock: `x/gateway`, `x/observability`, `x/rest`, and
  `x/websocket` are the beta extension families for v1. All remaining `x/*`
  modules stay experimental until release history, API snapshots, and owner
  sign-off blockers are cleared in `specs/extension-beta-evidence.yaml`.
- Stable API snapshots:
  - `docs/stable-api/snapshots/core-head.snapshot`
  - `docs/stable-api/snapshots/router-head.snapshot`
  - `docs/stable-api/snapshots/contract-head.snapshot`
  - `docs/stable-api/snapshots/middleware-head.snapshot`
  - `docs/stable-api/snapshots/security-head.snapshot`
  - `docs/stable-api/snapshots/store-head.snapshot`
  - `docs/stable-api/snapshots/health-head.snapshot`
  - `docs/stable-api/snapshots/log-head.snapshot`
  - `docs/stable-api/snapshots/metrics-head.snapshot`
- Extension snapshots:
  - `docs/extension-evidence/snapshots/first-batch/x-rest-head.snapshot`
  - `docs/extension-evidence/snapshots/first-batch/x-websocket-head.snapshot`
  - `docs/extension-evidence/snapshots/first-batch/x-tenant-head.snapshot`
  - `docs/extension-evidence/snapshots/first-batch/x-observability-head.snapshot`
  - `docs/extension-evidence/snapshots/first-batch/x-gateway-head.snapshot`
  - `docs/extension-evidence/snapshots/ai-stable-tier/x-ai-provider-head.snapshot`
  - `docs/extension-evidence/snapshots/ai-stable-tier/x-ai-session-head.snapshot`
  - `docs/extension-evidence/snapshots/ai-stable-tier/x-ai-streaming-head.snapshot`
  - `docs/extension-evidence/snapshots/ai-stable-tier/x-ai-tool-head.snapshot`

## Local Gate Evidence

- Evidence package card: `tasks/cards/done/1138-v1-release-evidence-package.md`.
- Baseline before packaging: `2f24c6d8` (`Complete extension maturity lock`).
- Local release gate: `GOCACHE=/private/tmp/plumego-gocache make gates` passed on May 2, 2026.
- V1 cleanup final gate: `GOCACHE=/private/tmp/plumego-gocache GOMODCACHE=/private/tmp/plumego-gomodcache make gates` passed on May 14, 2026 after Phase 2-4 cleanup cards `1394` through `1402`.
- Stable-root v1 freeze re-audit: `go run ./internal/checks/dependency-rules`, `go run ./internal/checks/deprecation-inventory -strict`, and `bash scripts/check-stable-api-snapshots.sh` passed on May 15, 2026; no new stable API shape changes or unregistered stable compatibility markers were introduced.
- V1 cleanup extension evidence: `go run ./internal/checks/extension-beta-evidence` and `go run ./internal/checks/extension-maturity` passed on May 14, 2026; beta families remain `x/gateway`, `x/observability`, `x/rest`, and `x/websocket`.
- Deprecation inventory gate: `GOCACHE=/private/tmp/plumego-gocache go run ./internal/checks/deprecation-inventory -strict` passed on May 1, 2026.
- Generated website data: `make gates` ran `website/pnpm sync`, compared `website/src/generated`, and completed `pnpm check` plus `pnpm build` without generated-file drift.
- M-005 control-plane evidence: cards 1429 and 1430 write the release execution
  plan, milestone verify artifacts, and rc tag status before final v1 decision.
- M-005 rc gate: `GOCACHE=/private/tmp/plumego-gocache GOMODCACHE=/private/tmp/plumego-gomodcache make gates` passed on May 15, 2026 after blocker card 1435; stable-module coverage was 87.2%.
- M-005 stable-root final freeze: `go run ./internal/checks/deprecation-inventory -strict`, `go test -race -timeout 60s ./contract ./core ./router ./middleware/... ./security/... ./store/... ./health ./log ./metrics`, and `go vet ./contract ./core ./router ./middleware/... ./security/... ./store/... ./health ./log ./metrics` passed on May 15, 2026.
- M-005 CLI onboarding evidence: `cd cmd/plumego && go test -timeout 20s ./...`,
  `cd cmd/plumego && go vet ./...`, and `cd cmd/plumego && go run . new
  --template canonical --dry-run trust-check` passed on May 15, 2026.
- M-005 extension maturity boundary: `go run ./internal/checks/extension-maturity`
  and `go run ./internal/checks/extension-beta-evidence` passed on May 15,
  2026. Beta remains limited to `x/gateway`, `x/observability`, `x/rest`, and
  `x/websocket`; remaining candidates keep explicit evidence blockers.
- M-005 final observation gate: `GOCACHE=/private/tmp/plumego-gocache
  GOMODCACHE=/private/tmp/plumego-gomodcache make gates` passed on May 15,
  2026 after blocker card 1437; stable-module coverage remained 87.2%.
- Final tag command: `git tag -a v1.0.0-rc.1 -m "Plumego v1.0.0-rc.1"` from the
  reviewed M-005 release-execution commit after local gates pass.

## Observation Window

- Local tag status: `v1.0.0-rc.1` exists locally. `git rev-parse --short
  v1.0.0-rc.1` returned tag object `151770bf`; `git rev-parse --short
  v1.0.0-rc.1^{}` returned target commit `a2346813`.
- Final v1 status: `v1.0.0` is not tagged by this release candidate card.
- Remote GitHub gate status: passed. `v1.0.0-rc.1` triggered GitHub Actions run
  `25920615874`; `website-quality-gates`, `go-submodule-quality-gates
  (cmd/plumego)`, `go-quality-gates`, and `v1-release-readiness` all completed
  successfully.
- Observation status: passed by card 1436 on May 15, 2026. Remote run
  `25920615874` remained successful, open GitHub issues were empty, and the
  only open PR was unrelated to the v1 release blocker set.
- Final v1 handoff: `v1.0.0` is ready to tag from the committed release
  decision state.
- Final v1 result: `v1.0.0` was tagged after the observation window. GitHub
  Actions run `25922384589` passed for the final tag.
- Extension evidence status: beta evidence is complete for `x/gateway`,
  `x/observability`, `x/rest`, and `x/websocket` using release refs
  `d2c25c3` and `ec70358`. Remaining extension candidates still need their
  own release refs, API snapshots, and owner sign-off before beta promotion.

## Gate Commands

Run from the repository root:

```bash
make gates
go run ./internal/checks/dependency-rules
go run ./internal/checks/agent-workflow
go run ./internal/checks/module-manifests
go run ./internal/checks/reference-layout
go run ./internal/checks/extension-maturity
go run ./internal/checks/extension-beta-evidence
go run ./internal/checks/deprecation-inventory -strict
go vet ./...
gofmt -l .
go test -race -timeout 60s ./...
go test -timeout 20s ./...
cd cmd/plumego && go vet ./...
cd cmd/plumego && go test -race -timeout 60s ./...
cd cmd/plumego && go test -timeout 20s ./...
cd website && pnpm sync
cd website && pnpm check
cd website && pnpm build
```

Expected result: all commands pass, `gofmt -l .` prints nothing, and `pnpm sync`
does not leave uncommitted generated-file changes.

## Known Blockers

- No additional extension beta promotion is included beyond `x/gateway`,
  `x/observability`, `x/rest`, and `x/websocket`.
- Tagged CLI install is not advertised for this rc. The smoke command
  `go install github.com/spcent/plumego/cmd/plumego@v1.0.0-rc.1` resolves the
  root module tag but fails because `cmd/plumego` is a nested Go module; use a
  source checkout for CLI validation.
- `specs/extension-beta-evidence.yaml` still records
  `release_history_missing`, `api_snapshot_missing`, or `owner_signoff_missing`
  blockers for remaining experimental candidates and sub-surfaces.

## Tagging

After the release-readiness branch is committed and all gate commands pass:

```bash
git tag -a v1.0.0-rc.1 -m "Plumego v1.0.0-rc.1"
```

After tagging, confirm:

```bash
git tag -l 'v1.0.0-rc.1'
git rev-parse v1.0.0-rc.1
```
