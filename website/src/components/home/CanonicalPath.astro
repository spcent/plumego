---
const { copy } = Astro.props;
---

<section class="section">
  <div class="section__intro">
    <h2>{copy.canonicalTitle}</h2>
    <p>{copy.canonicalBody}</p>
    <a class="text-link" href={copy.canonicalCta.href}>{copy.canonicalCta.label}</a>
  </div>
  <div class="canonical-layout">
    <div class="canonical-blueprint">
      <p class="signal-list__kicker">canonical shape</p>
      <h3>reference/standard-service</h3>
      <pre class="blueprint-tree"><span class="bt-dir">reference/standard-service/</span>
├── <span class="bt-file">main.go</span>
├── <span class="bt-dir">internal/app/</span>
│   ├── <span class="bt-file">app.go</span>
│   └── <span class="bt-file">routes.go</span>
└── <span class="bt-dir">internal/handler/</span>
    └── <span class="bt-file">health.go</span></pre>
    </div>
    <div class="step-list">
      {copy.canonicalSteps.map((step: { label: string; title: string; body: string }) => (
        <article class="step-list__item">
          <p class="signal-list__kicker">{step.label}</p>
          <h3>{step.title}</h3>
          <p>{step.body}</p>
        </article>
      ))}
    </div>
  </div>
</section>
