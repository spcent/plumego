---
title: Adoption Path
description: The narrow step-by-step path for new Plumego users from first service to full control-plane understanding.
translationKey: guides-adoption-path
sidebar:
  order: 17
---

# Adoption Path

This page is the narrow adoption path for new Plumego users.

The first impression should be simple: standard-library HTTP compatibility, small stable kernel, explicit wiring, and machine-checkable agent workflow. Optional `x/*` packages are capability additions, not alternate bootstraps.

## 5 minutes: run one standard service

Read:

1. [Getting Started](/docs/getting-started)
2. `reference/standard-service/README.md`

Use:

- `core.DefaultConfig`
- `core.New`
- `app.Use`
- `app.Get`
- `contract.WriteResponse`
- `contract.WriteError`

Avoid:

- `x/*` packages
- custom response envelopes
- hidden route registration

Done when a local service responds from one explicit route using `http.HandlerFunc`.

## 30 minutes: add one capability

Keep `reference/standard-service` as the app shape. Add one capability family:

| Need | First read |
| --- | --- |
| CRUD / resource conventions | [x/rest Primer](/docs/modules/x-rest) |
| Tenant resolution and isolation | [x/tenant Primer](/docs/modules/x-tenant) |
| Edge proxy and rewrite | [x/gateway Primer](/docs/modules/x-gateway) |
| WebSocket transport | [x/websocket Primer](/docs/modules/x-websocket) |
| Observability exporters | [x/observability Primer](/docs/modules/x-observability) |
| AI provider / session / tool primitives | [x/ai Primer](/docs/modules/x-ai) |

Done when the app still has explicit route wiring and only the selected capability family is added.

## 1 day: understand the control plane

Read:

1. `AGENTS.md`
2. [Canonical Style Guide](/docs/guides/style-guide)
3. [Agent-First Workflow](/docs/concepts/agent-first-workflow)
4. `specs/repo.yaml`
5. `specs/dependency-rules.yaml`
6. the target module's `module.yaml`

Done when a developer or agent can answer:

- which module owns the change
- which paths are out of scope
- whether stable public APIs are affected
- which validation commands must pass

## Messaging rule

When updating README, website, or onboarding docs, present Plumego in this order:

1. standard-library compatibility
2. small stable kernel
3. explicit reference application path
4. machine-checkable agent workflow
5. optional experimental capability catalog

## Read next

- [Reference App](/docs/reference-app) — the canonical service shape to copy from
- [Stable Roots](/docs/stable-roots) — which packages carry the long-term promise
- [x/* Family](/docs/x-family) — how optional capabilities are organized
- [When Not to Use Plumego](/docs/when-not-to-use) — honest trade-off assessment
