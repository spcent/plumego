---
title: Modules Overview
description: Stable roots stay narrow, and extension families live under x/*.
translationKey: modules-overview
sidebar:
  order: 4
---

import GeneratedModuleFacts from '../../../../components/docs/GeneratedModuleFacts.astro';
import RequestFlowViz from '../../../../components/marketing/RequestFlowViz.astro';

# Modules Overview

Plumego keeps long-lived public packages at the repository root and places optional or fast-moving capabilities under `x/*`. This split is intentional: it keeps the stable surface small, keeps ownership easier to classify, and prevents the default learning path from being redefined by extension work.

## Tier summary

| Tier | Count | Adoption guidance |
| --- | --- | --- |
| **Stable** (GA) | 9 packages | Production-ready. v1 compatibility promise applies. |
| **Beta** | 7 families | API frozen between consecutive minor release refs. Production-safe with release-note checks before upgrading. |
| **Experimental** | 7 primary families | API may change without a deprecation period. Wrap behind an app-local interface in stable service paths. |

Full maturity evidence, per-extension dashboard, and release matrix: [Status →](/status)

---

## Synced module inventory

<GeneratedModuleFacts />

## Repository layer map

<RequestFlowViz
  nodes={[
    { step: '1', title: 'Default service path', code: 'stable roots', note: 'kernel, router, contract, middleware', accent: true },
    { step: '2', title: 'Runnable shape', code: 'reference app', note: 'bootstrap, route wiring, handlers' },
    { step: '3', title: 'Capability work', code: 'x/*', note: 'optional families, start from the primary entrypoint' },
    { step: '4', title: 'Control plane', code: 'docs/specs/tasks', note: 'intent, machine rules, execution scope', accent: true },
  ]}
/>

```text
stable roots
  core router contract middleware security store health log metrics

x/* families
  optional capability and protocol surfaces

control plane
  reference docs specs tasks module.yaml
```

## Classification matrix

| Work type | Start here | Why |
| --- | --- | --- |
| Kernel, lifecycle, route structure, transport contracts, or stable middleware | [Stable Roots](/docs/stable-roots) | This is the default path every service should understand |
| Capability, protocol, tenant, gateway, data-topology, or observability-adapter work | [x/* Family](/docs/x-family) | Optional capability work should not expand stable roots |
| App-local bootstrap and service wiring | [Reference App](/docs/reference-app) | The canonical service shape lives outside runtime package ownership |
| Ownership rules, routing taxonomy, import boundaries, or execution scope | [Repository Boundaries](/docs/concepts/repo-control-plane) | These are docs/specs/tasks control-plane questions |

## Extension entrypoint rule

| If the work is about | Start at | Then go narrower when |
| --- | --- | --- |
| Messaging, queues, pubsub, scheduling, or webhooks | `x/messaging` | The task is clearly scheduler-, webhook-, pubsub-, or mq-specific |
| Edge transport, reverse proxying, or load balancing | `x/gateway` | The task is clearly discovery- or ipc-specific |
| Reusable resource-interface work | `x/rest` | You are inside a specific CRUD/resource controller concern |
| Higher-level observability adapters | `x/observability` | The task is clearly ops- or devtools-specific |
| Data topology, sharding, durable KV, or read/write splitting | `x/data` | The task is clearly cache-specific |

## Module primers to open next

Once the top-level question is clear, continue into the primer that matches the owning module.

### Stable roots — `ga`

These modules carry the v1 stable-root compatibility promise. They are the recommended first scope for production evaluation.

| If the question is mainly about | Open this primer next | Why this is the right next read |
| --- | --- | --- |
| app construction, lifecycle, and route attachment | [Core Primer](/docs/modules/core) · **Stable** | `core` owns the HTTP application kernel and the canonical lifecycle path |
| HTTP transport wrappers, request IDs, tracing, rate limiting, and access logging | [Middleware Primer](/docs/modules/middleware) · **Stable** | `middleware` keeps ordering-sensitive HTTP behavior narrow and explicit |
| route matching, path parameters, route groups, and reverse routing | [Router Primer](/docs/modules/router) · **Stable** | `router` owns the trie-based route matching surface |
| request/response contracts, error model, context helpers, and trace metadata | [Contract Primer](/docs/modules/contract) · **Stable** | `contract` defines the canonical error and response shape every handler uses |
| auth primitives, JWT, password helpers, security headers, and input validation | [Security Primer](/docs/modules/security) · **Stable** | `security` provides fail-closed authentication and validation primitives |
| storage contracts, file abstractions, cache primitives, and idempotency contracts | [Store Primer](/docs/modules/store) · **Stable** | `store` defines the stable persistence layer before topology-heavy data concerns begin |
| liveness and readiness model types and health aggregation | [Health Primer](/docs/modules/health) · **Stable** | `health` owns the component checker contracts and readiness state model |
| base logging contracts and structured logger wiring | [Log Primer](/docs/modules/log) · **Stable** | `log` defines the logging surface that extensions and stable roots share |
| base metrics contracts and collector registration | [Metrics Primer](/docs/modules/metrics) · **Stable** | `metrics` defines the metrics surface without owning exporter or adapter behavior |

### Extension families

Start from the primary family entrypoint, not from subordinate packages. Use the maturity tier as your adoption signal.

#### Beta

API is frozen between consecutive minor release refs. Safe to adopt in production; check release notes before upgrading.

| If the question is mainly about | Open this primer next | Promoted | Subordinate packages |
| --- | --- | --- | --- |
| reusable CRUD transport and resource route standardization | [x/rest Primer](/docs/modules/x-rest) | v0.2.0 | — |
| real-time WebSocket connection lifecycle, hub management, or auth | [x/websocket Primer](/docs/modules/x-websocket) | v0.2.0 | — |
| reverse proxying, gateway routing, load balancing, or edge transport | [x/gateway Primer](/docs/modules/x-gateway) | v0.2.0 | [x/gateway/discovery](/docs/modules/x-discovery) · `x/gateway/ipc` (experimental) |
| Prometheus exporters, OTel wiring, record buffering, or DB analytics | [x/observability Primer](/docs/modules/x-observability) | v0.2.0 | [x/observability/ops](/docs/modules/x-ops) · [x/observability/devtools](/docs/modules/x-devtools) (experimental) |
| multi-tenant resolution, policy, quota, rate limiting, or session | [x/tenant Primer](/docs/modules/x-tenant) | v1.1.0 | `resolve`, `policy`, `quota`, `ratelimit`, `session`, `store` |
| static asset serving, embedded FS, or SPA fallback routing | [x/frontend Primer](/docs/modules/x-frontend) | v1.1.0 | — |
| app-facing queue, pubsub, scheduler, or webhook orchestration | [x/messaging Primer](/docs/modules/x-messaging) | v1.1.0 (app-facing) | [scheduler](/docs/modules/x-scheduler) · [webhook · pubsub · mq](/docs/modules/x-messaging-subordinates) (subordinate primitives are experimental) |

#### Beta surfaces in experimental families

The parent family root is still experimental, but these subpackages have release-backed API stability at v1.1.0.

| Beta surfaces | Parent family primer | Notes |
| --- | --- | --- |
| `x/ai/provider` · `x/ai/session` · `x/ai/streaming` · `x/ai/tool` | [x/ai Primer](/docs/modules/x-ai) | Use these subpackages for stable AI work; remaining subpackages and the root family are experimental |
| `x/data/file` · `x/data/idempotency` | [x/data Primer](/docs/modules/x-data) | Use these subpackages for stable file and idempotency work; topology surfaces and `x/data/cache` remain experimental |

#### Experimental

API compatibility is not frozen. Evaluate before adopting; wrap behind an app-local interface in stable service paths.

| If the question is mainly about | Open this primer next | Subordinate packages |
| --- | --- | --- |
| AI provider adapters, session, streaming, or tool invocation | [x/ai Primer](/docs/modules/x-ai) | `provider`, `session`, `streaming`, `tool` are beta surfaces — see above; `orchestration`, `semanticcache`, `distributed`, `resilience` remain experimental |
| read-write splitting, sharding, durable KV, or idempotency topology | [x/data Primer](/docs/modules/x-data) | [x/data/cache](/docs/modules/x-cache); `file` and `idempotency` are beta surfaces — see above |
| HTTP transport for file upload, download, or streaming delivery | [x/fileapi Primer](/docs/modules/x-fileapi) | — |
| shared circuit breakers, retry wrappers, or resilience adapters across extensions | [x/resilience Primer](/docs/modules/x-resilience) | — |
| gRPC + HTTP service hosting or outbound RPC client pooling | [x/rpc Primer](/docs/modules/x-rpc) | — |
| OpenAPI 3.1 document generation from registered routes | [x/openapi Primer](/docs/modules/x-openapi) | — |
| explicit JSON binding and caller-owned request validation | [x/validate Primer](/docs/modules/x-validate) | — |

## What this page should help you avoid

The current repo actively discourages broad or ambiguous roots such as `ai`, `tenant`, `rest`, `pubsub`, `frontend`, `utils`, or `validator` at the top level. If the name sounds like a feature catalog or a vague bucket, it is probably the wrong place to begin.

## Read next

- [API Quick Reference](/docs/reference) — complete function signatures for core, contract, and router
- [Stable Roots](/docs/stable-roots)
- [x/* Family](/docs/x-family)
- [Core Primer](/docs/modules/core)
- [x/rest Primer](/docs/modules/x-rest)
- [Repository Boundaries](/docs/concepts/repo-control-plane)
