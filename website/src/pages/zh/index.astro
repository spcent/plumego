---
import FinalCta from '../../components/home/FinalCta.astro';
import Hero from '../../components/home/Hero.astro';
import ValueProps from '../../components/home/ValueProps.astro';
import MarketingLayout from '../../layouts/MarketingLayout.astro';
import { HOME_COPY } from '../../data/site';

const copy = HOME_COPY.zh;
---

<MarketingLayout
  locale="zh"
  title="显式 Go HTTP 服务"
  description="一个面向显式 HTTP 服务的 stdlib-first Go Web 工具包，强调收敛稳定根、可见 wiring 与 canonical 路径。"
  currentPath="/zh"
>
  <Hero copy={copy} />
  <ValueProps copy={copy} />
  <FinalCta copy={copy} />
</MarketingLayout>
