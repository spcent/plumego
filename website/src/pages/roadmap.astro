---
import MarketingLayout from '../layouts/MarketingLayout.astro';
import PageHero from '../components/marketing/PageHero.astro';
import { ROADMAP_COPY } from '../data/site';
import { ROADMAP_FACTS } from '../generated/roadmap';

const copy = ROADMAP_COPY.en;
---

<MarketingLayout locale="en" title={copy.title} description={copy.description} currentPath="/roadmap">
  <PageHero
    eyebrow={copy.eyebrow}
    title={copy.title}
    description={copy.description}
    links={[
      { label: 'Docs', href: '/docs' },
      { label: 'Releases', href: '/releases' },
    ]}
  />

  <section class="section">
    <div class="section__intro">
      <h2>{copy.introTitle}</h2>
      <p>{copy.introBody}</p>
    </div>
    <div class="signal-list">
      {copy.guideCards.map((item) => (
        <article class="signal-list__item">
          <p class="signal-list__kicker">{item.kicker}</p>
          <h3>{item.title}</h3>
          <p>{item.body}</p>
        </article>
      ))}
    </div>
  </section>

  <section class="section">
    <div class="section__intro">
      <h2>{copy.timelineTitle}</h2>
      <p>{copy.timelineBody}</p>
    </div>
    <div class="page-grid">
      {[
        { title: copy.sections[0].title, items: ROADMAP_FACTS.currentBaseline },
        { title: copy.sections[1].title, items: ROADMAP_FACTS.inProgress },
        { title: copy.sections[2].title, items: ROADMAP_FACTS.planned },
        { title: copy.sections[3].title, items: copy.sections[3].items },
      ].map((section) => (
        <article class="page-panel">
          <h2>{section.title}</h2>
          <ul>
            {section.items.map((item) => <li>{item}</li>)}
          </ul>
        </article>
      ))}
    </div>
  </section>

  <section class="section">
    <div class="section__intro">
      <h2>{copy.orderTitle}</h2>
      <p>{copy.orderBody}</p>
    </div>
    <div class="fact-stack">
      <article class="page-panel">
        <ul>
          {ROADMAP_FACTS.suggestedExecutionOrder.map((item) => <li>{item}</li>)}
        </ul>
      </article>
    </div>
  </section>

  <section class="section">
    <div class="section__intro">
      <h2>Use roadmap with release posture</h2>
      <p>Roadmap shows direction. Release posture tells you what can actually be adopted today.</p>
    </div>
    <div class="page-links">
      <a class="text-link" href="/releases">Inspect releases</a>
      <a class="text-link" href="/docs/release-posture">Read release posture</a>
      <a class="text-link" href="/docs/reference-app">Open the reference app</a>
    </div>
  </section>
</MarketingLayout>
